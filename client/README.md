# BattleBoard Next.js Client

This is a minimal Next.js frontend for the BattleBoard project.

Development:

  cd client
  npm install
  npm run dev

It connects back to the server via socket.io on the same host, so run the server (`node server.js`) first.